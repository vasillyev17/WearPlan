@model IEnumerable<WP.Models.Subscriber>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.idClient)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.status)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.idClient)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.status)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.idSubscriber">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.idSubscriber">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.idSubscriber">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
