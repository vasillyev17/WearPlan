@page
@model Attempt3.Pages.PurchasePage.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Purchase[0].idCustomer)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Purchase[0].idProduct)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Purchase[0].date)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Purchase) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.idCustomer)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.idProduct)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.date)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.idPurchase">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.idPurchase">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.idPurchase">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
