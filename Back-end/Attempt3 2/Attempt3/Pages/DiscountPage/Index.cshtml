@page
@model Attempt3.Pages.DiscountPage.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Discount[0].idProduct)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Discount[0].discount)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Discount) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.idProduct)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.discount)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.idDiscount">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.idDiscount">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.idDiscount">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
