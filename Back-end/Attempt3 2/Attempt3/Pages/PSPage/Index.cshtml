@page
@model Attempt3.Pages.PSPage.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.ProductSize[0].idProduct)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ProductSize[0].idSize)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.ProductSize) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.idProduct)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.idSize)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.idProductSize">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.idProductSize">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.idProductSize">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
