@page
@model Attempt3.Pages.PBPage.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.ProductBrand[0].idProduct)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ProductBrand[0].idBrand)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.ProductBrand) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.idProduct)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.idBrand)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.idProductBrand">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.idProductBrand">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.idProductBrand">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
