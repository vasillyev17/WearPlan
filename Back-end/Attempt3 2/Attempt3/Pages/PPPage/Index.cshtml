@page
@model Attempt3.Pages.PPPage.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.ProductPurchase[0].idProduct)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ProductPurchase[0].idPurchase)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.ProductPurchase) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.idProduct)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.idPurchase)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.idProductPurchase">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.idProductPurchase">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.idProductPurchase">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
