@page
@model Attempt3.Pages.ClientPage.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Client[0].shopName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Client[0].email)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Client[0].password)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Client) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.shopName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.email)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.password)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.idClient">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.idClient">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.idClient">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
